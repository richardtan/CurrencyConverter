-- Below are the default entries for the database currconvert.db

CREATE TABLE "android_metadata" ("locale" TEXT DEFAULT 'en_US');

INSERT INTO "android_metadata" VALUES ('en_US');

CREATE TABLE "dashboard" ("type" INTEGER, "curr_index" INTEGER, "value" TEXT, "base" INTEGER, "ordering" INTEGER);

INSERT INTO "dashboard" VALUES (1,82,'N/A',1, 1);

